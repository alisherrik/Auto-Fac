@model Auto_Fac.ViewModels.DepartanemtProfessionViewModel

@{
    ViewBag.Title = "title";
    Layout = "_LayoutAdmin";
}
<div class="container-fluid">
    <div class="card-header">
            <div class="card-title"> Кафедраи @Model.Departament.Name </div>
        </div>
        <div class="card-body">
            <div class="row">
                @foreach (var item in Model.EnumerableProfession)
                {
                    <div class="col-md-4 " style="margin-bottom:10px;">
                        <div class="card card-default">
                            <div class="card-body">
                                <h3 class="card-title">@item.Name</h3>
                                <a class="btn btn-info" asp-controller="Manage" asp-action="GetAllProfessions" asp-route-id="@Model.Departament.id">Go somewhere</a>
                            </div>
                        </div>
                    </div>
                }
                <div class="col-md-4" style="margin-bottom:10px;">
                    <div class="card card-default">
                        <div class="card-body">
                            <a class="btn btn-success form-control" asp-controller="Admin" asp-action="CreateProfessionView" asp-route-id="@Model.Departament.id"> + New Profession</a>
                        </div>
                    </div>
                </div>
                    
            </div>
                
        </div>
    </div>
